import React from 'react';
import BasicLayout from '../../Layout/layout';

const Upload = props => {
  return(
    <div>
      <BasicLayout>
        upload
      </BasicLayout>
    </div>
  )
};

export default Upload;