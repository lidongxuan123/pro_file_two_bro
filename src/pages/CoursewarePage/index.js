import React from 'react';
import BasicLayout from '../../layouts/index';

const Upload = props => {
  return(
    <div>
      <BasicLayout>
        upload
      </BasicLayout>
    </div>
  )
};

export default Upload;